## プロジェクト概要
このプロジェクトは家庭内で使用する一週間の献立の作成と献立に必要な買い物メモを作成するのもです。
想定するユーザーとしては家庭で献立作成から買い物をしている人となります。

## 技術スタック
- フロントエンド
    - TypeScript
    - React
    - Vue.js
- バックエンド
    - Go
    - Gin
- DB
    - PosgreSQL
- UIライブラリ
    - Tailwind CSS

## コーディング規約・ルール

### フロントエンド

- 基本方針: Vue 3 Composition API (`<script setup>`) を使用する。
- 命名規則:
    - コンポーネント名: `PascalCase` (例: `UserProfile.vue`)
    - 変数・関数名: `camelCase`
    - 型・インターフェース: `PascalCase` (Prefixの I は必要)
    - ディレクトリ構成: `src/components`, `src/composables`, `src/stores (Pinia)` 等に適切に分割する。

### バックエンド(Go)

- 基本方針：`Effective Go`及び`User Go Style Guide`に準拠する
- 命名規則：
    - エクスポートする関数・構造体：`PascalCase`
    - 内部変数・非公開関数：`camelCase`
    - 略語は`URL`,`ID`,`JSON`のように大文字を維持する
- エラーハンドリング：早期リターン(`if err != nil`)を徹底し、`panic`は原則使用しない

### データベース(PosgreSQL)
- 命名規則：テーブル名、から無名ともに`snake_case`を使用する
- 基本：外部キー制約、インデックス、`NOT NULL`制約を適切に設定する

## 開発プロセス（TDD：テスト駆動開発）
本プロジェクトでは**TDD**を必須とする。コード生成・提案時は以下のステップを遵守すること。
1. Red(Test First)：実際のコードの前に、要求を満たす最小限のテストコードを提示する。
2. Green(Implementation)：テストをパスするための最短・最小限の実装を行う。
3. Refactor：グリーン状態を維持したまま、コードの品質（可読性・保守性）を向上させる。

## テストツール
- フロントエンド：`Vitest`, `Vue Test Utils`
- バックエンド：testing(標準パッケージ), testify/assert
    - Goでは**テーブル駆動テスト(Table-Driven Tests)** 形式で記述すること
    - DBが絡む場合は`sqlmock`または`testcontainers-go`を検討する

## コミットメッセージ規約
以下のフォーマットでメッセージを作成すること
- フォーマット：`機能名｜prefix: 対応内容`
- Prefix一覧：
    - `add`：新機能・ファイルの追加
    - `fix`：バグ修正
    - `update`：機能改善・仕様変更
    - `refactor`：コード整理（挙動変更なし）
    - `docs`：ドキュメント関連
- 例：`在庫管理｜add: 商品登録時のバリデーションテストと実装`

## AIへの振る舞い指示
- 回答は簡潔に保ち、詳細な説明が必要な場合のみ補足すること
- コードを提案する際は、常に上記のTDDプロセスとコーディング規約を反映させること
- 要件に不明瞭な点や、設計上の判断が必要な「不安な点」がある場合は、**作業（コード生成）を開始する前に必ずユーザーに確認し、合意を得ること**
- **データ構造の定義**：すべてのデータモデル、DBスキーマ、型定義は`.docs/schema.md`を唯一の正解(SSOT)とする。
- **変更フロー**
    - データ構造の変更が必要な場合は、実装コードを修正する前に必ず`.docs/schema.md`を更新すること
    - 更新後、他のエージェントやユーザーに対して「スキーマを更新した」旨を明示的に通知すること。
- **参照義務**：データを扱う実装を行う際は、必ず`.docks/schema.md`を読み込み、定義されている方や制約に従うこと

### 役割別ガイドライン

A. フロントエンド担当(Antigravity)
- 目的：`.docs/specs/*.md`に定義された機能要件を満たす、方安全で保守性の高いUIを構築する
- データ参照：実装前に必ず`.docs/schema.md`を参照し、定義された型と命名規則を厳守すること
- 依存関係の解決：バックエンドのAPIが未実装の場合は、`.docs/schema.md`の定義に基づいたモックデータを使用して開発を進めること

B. バックエンド担当（gemini-cli）
- 目的：`.docs/schema.md`に定義されたデータ構造を厳守し、`.docs/specs/*.md`のロジックを正確に実施する
- 整合性の維持：APIのレスポンス形式が`.docs/schema.md`や`.docs/specs/`の定義と１文字でも食い違わないように最新の注意を払うこと
- 技術スタック：指定された技術スタック（Go, TypeScript)のベストプラクティスに従い、拡張可能な設計を行うこと

### 作業フロー
1. Read: `GEMINI.md`, `.docs/spec.md`, `.docs/schema.md`を読み込み、現在の文脈を理解する。
2. Think：`.docs/specs/*.md`を確認し、実装すべきテストケースを想定する。
3. Plan：実装に着手する前に、以下の内容をユーザーに提示し承認を得ること
    - 修正・新規作成するファイルの一覧
    - 採用するロジックやアルゴリズムの概要
    - `.docs/schema.md`の変更が必要かどうかの判断
4. Update：プランが承認され、かつスキーマ変更が必要な場合は、まず`.docs/schema.md`を更新する。
5. Execute：承認されたプランに基づき、TDDのサイクル(テスト作成→実装)でコードを生成する。
